version: "3.9"

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      PYTHONUNBUFFERED: "1"
      LEAN_EXECUTABLE: /root/.elan/bin/lean
    stdin_open: true
    tty: true
